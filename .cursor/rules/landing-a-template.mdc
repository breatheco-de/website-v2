---
globs: src/data/landing/*,src/templates/landing_a.js
alwaysApply: false
---
# Rule: Landing A Template Structure
Type: Workspace Rule
Description: Comprehensive guide for landing_a template pages with all available components, modifiable vs fixed properties, and styling guidelines.

## 0. File Naming Convention (Required)

### Language-Specific File Extensions
- **English (US) landing pages**: Use `.us.yml` extension
  - Example: `miami-coding-bootcamp.us.yml`
- **Spanish (ES) landing pages**: Use `.es.yml` extension
  - Example: `miami-coding-bootcamp.es.yml`
- **File names should be descriptive and use kebab-case**
  - ✅ `dallas-scholarship.us.yml`
  - ❌ `dallas_scholarship.us.yml` or `DallasScholarship.us.yml`

### Language Consistency
- All content within `.us.yml` files must be in English
- All content within `.es.yml` files must be in Spanish
- Maintain consistent terminology and tone within each language
- Use appropriate cultural references for target audience

## 1. YAML File Structure (Required)

### Component Ordering in YAML File
**CRITICAL: The order of components in the YAML file is NOT determined by position numbers. Follow this exact structure:**

```yaml
# 1. Required Components (Always First)
meta_info:
navbar:
form:
header_data:

# 2. Recommended Components (Second)
short_badges:
features:
follow_bar:

# 3. Optional Standalone Components (Third - ABOVE components section)
rating_reviews:
geeks_vs_others:
why_4geeks:
who_is_hiring:
badges:
program_details:
alumni_projects:
why_python:
in_the_news:
choose_your_program:

# 4. Components Section (Last - BELOW all standalone components)
components:
  - name: component_name
    position: 1
  - name: another_component
    position: 3
```

### Why This Structure Matters
- **YAML Parsing**: Prevents parsing errors and ensures proper component loading
- **Project Convention**: Maintains consistency across all landing pages
- **Component Dependencies**: Some standalone components may depend on others
- **Maintenance**: Makes files easier to read and maintain

### Position Numbers vs YAML Structure
- **Position numbers** (1, 3, 5, etc.) control the **display order** on the page
- **YAML structure** controls the **file organization** and **parsing order**
- You can have a `components` section item at position 1 and a standalone component at position 3, but the standalone component must still appear above the `components` section in the YAML file

## 2. Required Components (Fixed Structure)

### meta_info (Required - Fixed Keys, Modifiable Values)
```yaml
meta_info:
  slug: unique-landing-name        # MODIFIABLE: URL slug
  title: "SEO Title"              # MODIFIABLE: 50-60 characters
  description: "SEO Description"   # MODIFIABLE: 150-160 characters
  visibility: "visible"           # MODIFIABLE: visible, hidden, unlisted
  image: "/images/bg/image.jpg"   # MODIFIABLE: Social preview image path
  template: "landing_a"           # FIXED: Must be "landing_a"
  utm_course: ["full-stack"]      # MODIFIABLE: Course array
  utm_location: ["downtown-miami"] # MODIFIABLE: Location array
  automation: "soft"              # MODIFIABLE: soft, strong
  tag: "request_more_info"        # MODIFIABLE: Lead tag
  keywords: " "                  #KEEP EMPTY: (DEFAULT)
  redirects: []                  #KEEP EMPTY: (DEFAULT)

```

### navbar (Required - Fixed Keys, Modifiable Values)
```yaml
navbar:
  buttonText: "Apply Now"         # MODIFIABLE: CTA button text
  buttonUrl: "#leadform_header"   # MODIFIABLE: URL or section ID
  logoUrl: "#"                   # MODIFIABLE: Logo link
```

### form (Required - Fixed Keys, Modifiable Values)
```yaml
form:
  fields: ["full_name", "phone", "email"] # MODIFIABLE: Available fields
  heading: "Get Course Details"            # MODIFIABLE: Form title
  motivation: "Apply to receive info"      # MODIFIABLE: Form subtitle
  button_label: "Apply Now"               # MODIFIABLE: Submit button text
  redirect: "/us/thank-you/"              # MODIFIABLE: Post-submit redirect
  hide_form: false                        # MODIFIABLE: Hide bottom form
```

### header_data (Required - Fixed Keys, Modifiable Values)
```yaml
header_data:
  background: "#ffffff"           # MODIFIABLE: Background color
  tagline: "Main Headline"        # MODIFIABLE: Main headline text
  tagline_color: "#1c1c1c"       # MODIFIABLE: Headline color
  sub_heading: "Supporting text"  # MODIFIABLE: Subtitle text
  form_styles:                    # MODIFIABLE: Form styling
    background: "#FFB718"
    color: "black"
    button:
      background: "black"
  badge: "../../../static/images/badges/badge.webp" # MODIFIABLE: Optional badge
  image_filter: brightness(0.9)   # MODIFIABLE: Optional image filter
```

## 2. Recommended Components (Optional, Modifiable Content)

### short_badges (Recommended)
```yaml
short_badges:
  - name: "Award Name"            # MODIFIABLE: Badge description
    url: ""                      # MODIFIABLE: Badge link
    image: "../../../static/images/badges/badge.webp" # MODIFIABLE: Badge image
    alt: "Badge description"      # MODIFIABLE: Alt text
```

### features (Recommended)
```yaml
features:
  marginTop: 0px                  # MODIFIABLE: Top margin
  text: "Descriptive text"       # MODIFIABLE: Feature description
  bullets:                       # MODIFIABLE: Feature bullet points
    - "Feature one"
    - "Feature two"
  styles: '{ "fontSize": "20px" }' # MODIFIABLE: Custom styles
```

### follow_bar (Recommended)
```yaml
follow_bar:
  position: "top"                # MODIFIABLE: top, bottom, hidden
  content:
    text: "Main message"         # MODIFIABLE: Desktop text
    text_mobile: "Mobile msg"    # MODIFIABLE: Mobile text
    font_size: ["25px", "25px", "25px", "20px", "18px"] # MODIFIABLE: Responsive sizes
  button:
    text: "CTA Text"            # MODIFIABLE: Button text
    path: "#section_id"         # MODIFIABLE: Button link
  phone:
    text: "Call"               # MODIFIABLE: Phone label
    number: ""                 # MODIFIABLE: Phone number
```

## 3. Optional Standalone Components

**IMPORTANT: These components MUST be placed ABOVE the `components` section in the YAML file, regardless of their position numbers. This is required for proper YAML parsing and project consistency.**

### program_details (Optional)
```yaml
program_details:
  position: 2                    # MODIFIABLE: Component position
  heading: "What you'll learn"   # MODIFIABLE: Section title
  sub_heading: "Technologies"    # MODIFIABLE: Section subtitle
  background: "#FFFFFF"          # MODIFIABLE: Background color
```

### badges (Optional)
```yaml
badges:
  position: 5                    # MODIFIABLE: Component position
  heading: "Awards and recognitions" # MODIFIABLE: Section title
  sub_heading: "Excellence validation" # MODIFIABLE: Section subtitle
```

### rating_reviews (Optional)
```yaml
rating_reviews:
  background: "#FBFBFB"          # MODIFIABLE: Section background
  position: 4                    # MODIFIABLE: Component position
  heading: "Reviews heading"     # MODIFIABLE: Section title
  rating_list:                   # MODIFIABLE: Review platforms
    - alt: "Platform name"
      image: "../../../static/images/badges/platform.png"
      rating: 4.89              # MODIFIABLE: Rating score
      url: ""                   # MODIFIABLE: Review link
```

### why_4geeks (Optional)
```yaml
why_4geeks:
  position: 8                    # MODIFIABLE: Component position
  heading: "Why 4Geeks Academy"  # MODIFIABLE: Section title
  sub_heading: "Our advantages"  # MODIFIABLE: Section subtitle
```

### who_is_hiring (Optional)
```yaml
who_is_hiring:
  position: 9                    # MODIFIABLE: Component position
  limit: 4                       # MODIFIABLE: Number of companies shown
  heading: "Who's hiring our students?" # MODIFIABLE: Section title
  sub_heading: "Partner companies" # MODIFIABLE: Section subtitle
  background: "#FFFFFF"          # MODIFIABLE: Background color
```

### geeks_vs_others (Optional)
```yaml
geeks_vs_others:
  position: 7                    # MODIFIABLE: Component position
  heading: "4Geeks vs Others"    # MODIFIABLE: Section title
  paragraph: "Comparison description" # MODIFIABLE: Section description
  sub_heading: "Discover differences" # MODIFIABLE: Section subtitle
  total_rows: 5                  # MODIFIABLE: Number of comparison rows
```

### alumni_projects (Optional)
```yaml
alumni_projects:
  position: 8                    # MODIFIABLE: Component position
  heading: "Student projects"    # MODIFIABLE: Section title
  sub_heading: "Final projects"  # MODIFIABLE: Section subtitle
```

### why_python (Optional)
```yaml
why_python:
  position: 7                    # MODIFIABLE: Component position
  heading: "Why Python?"         # MODIFIABLE: Section title
  sub_heading: "Benefits"        # MODIFIABLE: Section subtitle
```

### in_the_news (Optional)
```yaml
in_the_news:
  position: 15                   # MODIFIABLE: Component position
  heading: "In the news"         # MODIFIABLE: Section title
  limit: 3                       # MODIFIABLE: Number of news items
```

### choose_your_program (Optional)
```yaml
choose_your_program:
  position: 10                   # MODIFIABLE: Component position
  heading: "Choose your program" # MODIFIABLE: Section title
  sub_heading: "Available options" # MODIFIABLE: Section subtitle
```

### apply_schollarship (Optional)
```yaml
apply_schollarship:
  imageSide: "right"            # MODIFIABLE: left, right
  image: "../../../static/images/landing/apply.png" # MODIFIABLE: Image path
```

## 4. Components Section (Fully Modifiable)

**YAML STRUCTURE REQUIREMENT: The `components` section MUST come AFTER all standalone components above, regardless of position numbers.**

### Structure and Positioning (Modifiable)
- Use non-consecutive position numbers (1, 3, 5, etc.) for easy reordering
- Each component requires unique name and position
- **Position numbers are for display order only, not YAML file structure**
```yaml
components:
  - name: unique_component_name   # MODIFIABLE: Component identifier
    position: 1                  # MODIFIABLE: Non-consecutive numbers
```

### Available Layouts (Fixed Options)
- `two_column_left` - Text left, image right
- `two_column_right` - Text right, image left
- `iconogram` - Icon grid layout
- `testimonials_new` - Testimonial carousel
- `multi_column` - Multiple column layout
- `single_column` - Single column layout
- `job_guarantee_small` - Job guarantee section (specialized layout)
- `faq` - FAQ section
- `choose_your_program` - Program selection
- `geeksInfo` - 4Geeks information section
- `divider` - Visual separator
- `simple_cards` - Card grid layout

### Specialized Layouts

#### job_guarantee_small (Specialized)
```yaml
- name: job_guarantee_component
  layout: job_guarantee_small
  position: 
  heading:
    text: "Enroll with a Job Guarantee"  # MODIFIABLE: Only heading.text is required
```
**Note**: This layout automatically includes job guarantee content and styling. Only the `heading.text` property is needed.

### Common Properties (Modifiable Values)
```yaml
proportions: [6, 6]             # MODIFIABLE: Column width ratios (must sum to 12)
background: "#FFFFFF"           # MODIFIABLE: Background color/name
layout: "two_column_left"       # MODIFIABLE: Layout type
swipable: false                 # MODIFIABLE: Mobile swipe functionality
```

### Image Properties (Modifiable)
```yaml
image:
  style: '{ "backgroundSize": "contain", "background-position": "center" }'
  src: "/images/landing/image.webp" # MODIFIABLE: Image path
  shadow: false                  # MODIFIABLE: Shadow effect
  link: "/example"              # MODIFIABLE: Image click link
```

### Video Support (Modifiable)
```yaml
video: "VIDEO_ID"               # MODIFIABLE: YouTube video ID
height: "400px"                 # MODIFIABLE: Video height
width: "100%"                   # MODIFIABLE: Video width
```

### Text Elements (Modifiable Content, Fixed Structure)
```yaml
heading:
  text: "Heading Text"          # MODIFIABLE: Heading content
  font_size: ["40px", "34px", "34px", "25px", "25px"] # MODIFIABLE: xl, lg, md, sm, xs
  style: '{ "lineHeight": "30px" }' # MODIFIABLE: Custom CSS

sub_heading:
  text: "Sub-heading text"      # MODIFIABLE: Subtitle content
  font_size: ["16px"]           # MODIFIABLE: Font sizes
  style: '{ "font-weight": "100" }' # MODIFIABLE: Custom CSS

section_heading:
  text: "Section heading"       # MODIFIABLE: Section title
  style: '{ "fontSize": "30px" }' # MODIFIABLE: Custom CSS

content:
  text: "Main content text"     # MODIFIABLE: Body content
  font_size: ["16px"]           # MODIFIABLE: Font sizes
  path: "/link"                 # MODIFIABLE: Optional link
```

### Bullets and Lists (Modifiable)
```yaml
bullets:
  item_style: '{ "padding": 0 }' # MODIFIABLE: Bullet item styling
  items:
    - heading: "Bullet heading"  # MODIFIABLE: Optional bullet title
      text: "Bullet text"       # MODIFIABLE: Bullet content
      icon: "check"             # MODIFIABLE: check, circle
      icon_color: "#25BF6C"     # MODIFIABLE: Icon color
```

### Button Properties (Modifiable)
```yaml
button:
  text: "Button Text"           # MODIFIABLE: Button label
  color: "white"                # MODIFIABLE: Text color
  background: "black"           # MODIFIABLE: Background color
  hover_color: "white"          # MODIFIABLE: Hover text color
  path: "#section"              # MODIFIABLE: Button link
  style: '{ "padding": "10px" }' # MODIFIABLE: Custom CSS
```

### Cards Layout (Modifiable)
```yaml
cards:
  - background: "#E0D1FF"       # MODIFIABLE: Card background
    heading:
      text: "Card title"        # MODIFIABLE: Card heading
      style: '{ "fontWeight": "700" }' # MODIFIABLE: Heading style
      font_size: "15px"         # MODIFIABLE: Font size
```

### Iconogram Layout (Modifiable)
```yaml
icons:
  - icon: "graduation-2"        # MODIFIABLE: Icon name
    color: "#0084FF"           # MODIFIABLE: Icon color
    title: "Icon Title"        # MODIFIABLE: Icon heading
    content: "Icon description" # MODIFIABLE: Icon text
```

### Divider Component (Modifiable)
```yaml
height: ["50px", "50px", "50px", "0px", "0px"] # MODIFIABLE: Responsive heights
```

## 5. Available Colors (Use Names or Hex)

### Common Colors
- `blue`: "#0084FF"
- `lightBlue`: "#BBEAFC"
- `lightBlue2`: "rgba(199, 243, 253, 0.5)"
- `darkBlue`: "#2E2E38"
- `blue2`: "#0097CD"
- `veryLightBlue`: "#C7F3FD"
- `veryLightBlue2`: "#E3F9FE"
- `veryLightBlue3`: "#F4F9FF"
- `yellow`: "#FFC718"
- `lightYellow`: "rgba(255, 183, 24, 0.1)"
- `lightYellow2`: "rgba(255, 183, 24, 0.2)"
- `darkYellow`: "#FFECBF"
- `green`: "#20630d"
- `lightGreen`: "#c4f7b7"
- `gray`: "#898a8b"
- `gray2`: "#A4A4A466"
- `gray3`: "#828282"
- `darkGray`: "#3A3A3A"
- `darkGray2`: "#606060"
- `darkGray3`: "#4D4D5C"
- `lightGray`: "#ebebeb"
- `lightGray2`: "#C4C4C4"
- `verylightGray`: "#F5F5F5"
- `verylightGray2`: "#FBFBFB"
- `verylightGray3`: "#F9F9F9"
- `borderGray`: "#ececec"
- `grayBrown`: "#333333"
- `black`: "#000000"
- `white`: "#FFFFFF"
- `red`: "red"
- `lightRed`: "#ffcdc9"
- `whitePink`: "#FFF1D1"

## 6. Text Formatting Rules

### Allowed Operators
- Use `|` for line breaks in YAML text fields
- Use `\n` for line breaks in follow_bar content
- HTML tags supported: `<br>`, `<b>`, `<strong>`, `<i>`, `<em>`, `<span>`

### Quote Usage (Critical for YAML Parsing)
- **ALWAYS use straight quotes (`"` and `'`) in YAML text fields**
- **NEVER use curly quotes (`"`, `"`, `'`, `'`) as they cause YAML parsing errors**
- Use double quotes (`"`) for text containing apostrophes: `"we've got the numbers"`
- Use single quotes (`'`) for simple text without apostrophes: `'Simple text'`
- **Examples:**
  - ✅ `text: "Yes — and we've got the numbers to back it up."`
  - ❌ `text: 'Yes — and we've got the numbers to back it up.'` (curly quotes cause errors)

### Style Objects (Fixed Format, Modifiable Values)
- Must use single quotes: `'{ "property": "value" }'`
- Must be valid JSON syntax
- Common properties: `fontSize`, `fontWeight`, `color`, `lineHeight`, `margin`, `padding`

### Image Paths (Fixed Format, Modifiable Paths)
- Use relative paths from static directory: `"/images/landing/"`
- Supported formats: `.webp` (preferred), `.png`, `.jpg`
- Badge path: `"../../../static/images/badges/"`

### Responsive Design (Fixed Breakpoints, Modifiable Values)
- Font sizes array: `[xl, lg, md, sm, xs]`
- Proportions must sum to 12: `[6, 6]`, `[4, 8]`, `[3, 9]`
- Test on all breakpoints

## 7. Best Practices

### YAML Structure Best Practices
- **ALWAYS place standalone components above the `components` section**
- **NEVER mix standalone components within the `components` section**
- **Use position numbers for display order, not file organization**
- **Follow the exact structure shown in Section 1**

### Correct vs Incorrect YAML Structure

#### ✅ CORRECT Structure:
```yaml
# Standalone components first
rating_reviews:
  position: 7
  heading: "Reviews"

geeks_vs_others:
  position: 5
  heading: "Comparison"

# Components section last
components:
  - name: first_component
    position: 1
  - name: second_component
    position: 3
```

#### ❌ INCORRECT Structure:
```yaml
components:
  - name: first_component
    position: 1
  - name: second_component
    position: 3

# Standalone components mixed in wrong place
rating_reviews:
  position: 7
  heading: "Reviews"

geeks_vs_others:
  position: 5
  heading: "Comparison"
```

### Content Guidelines
- Keep meta_info title 50-60 characters
- Keep meta_info description 150-160 characters
- Use descriptive alt text for images
- Optimize images before upload (use .webp format)

### Performance
- Keep style objects minimal
- Use component positions with gaps (1, 3, 5, 7...)
- Test form functionality
- Validate YAML syntax

### SEO Optimization
- Include relevant keywords in meta_info
- Write compelling titles and descriptions
- Use semantic heading structure
- Ensure mobile-friendly design

---

**Usage Notes:**
- **Start Minimal**: Begin with required components only
- **Add Incrementally**: Add optional components as needed
- **Test Responsively**: Verify on all device sizes
- **Validate Content**: Check YAML syntax and required fields
- **Modifiable vs Fixed**: Respect the structure while customizing content
